---
title: "每日一题 Leetcode  1.两数之和"
date: "2026-07-30 11:01:28"
description: ""
cover: "https://static.leetcode.cn/cn-legacy-assets/images/LeetCode_Sharing.png"
tags: ["leetcode"]
---



[2. 两数相加](https://leetcode.cn/problems/add-two-numbers/)

给你两个 **非空** 的链表，表示两个非负的整数。它们每位数字都是按照 **逆序** 的方式存储的，并且每个节点只能存储 **一位** 数字。

请你将两个数相加，并以相同形式返回一个表示和的链表。

你可以假设除了数字 0 之外，这两个数都不会以 0 开头。

 

**示例 1：**

![img](https://assets.leetcode.cn/aliyun-lc-upload/uploads/2021/01/02/addtwonumber1.jpg)

```
输入：l1 = [2,4,3], l2 = [5,6,4]
输出：[7,0,8]
解释：342 + 465 = 807.
```

**示例 2：**

```
输入：l1 = [0], l2 = [0]
输出：[0]
```

**示例 3：**

```
输入：l1 = [9,9,9,9,9,9,9], l2 = [9,9,9,9]
输出：[8,9,9,9,0,0,0,1]
```

 

```cpp
#include <iostream>
using namespace std;

/*

 
*/

ListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {
	ListNode *head,*p1,*p2,*p3,*end;
    p1 = l1,p2 = l2;
    head = nullptr;
    end = nullptr;
    
    int co = 0;
    while(p1 || p2 || co>0){

    int v1 = (p1 != nullptr) ? p1->val:0;
    int v2 = (p2 != nullptr) ? p2->val:0;
    int sum = v1 + v2 + co;

    co = sum/10;
    sum %=10;

    ListNode *newNode = new ListNode(sum);
    if(head == nullptr){
            head = newNode;
            end = newNode;
        }else {
            end->next=newNode;
            end=newNode;
        }
    
        if(p1) p1=p1->next;
        if(p2) p2=p2->next;
    }
	end = nullptr;
    return head;
}
// 通过 运行时间 1ms
int main(){




	return 0;
}

```

